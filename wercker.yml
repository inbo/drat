box: rocker/tidyverse
build_gh:
  steps:
    - add-ssh-key:
        keyname: drat
    - install-packages:
        packages: libgeos-dev libgdal-dev ssh r-cran-littler
    - script:
        name: render Rmd files
        code: ./render.sh
